Drop your video files here with these EXACT filenames and the site will pick them up automatically:

hero-bg.mp4              -> subtle looping background video behind the hero headline
case-automation.mp4      -> plays on hover over the "Lead Routing Automation" card
case-shopify.mp4         -> plays on hover over the "Shopify Storefront Rebuild" card
case-mvp.mp4             -> plays on hover over the "30+ Custom MVPs" card
case-support.mp4         -> plays on hover over the "AI Support Agent" card

Tips:
- Keep each clip short (5-10 sec, looping) and under ~3-5MB so the page stays fast.
- No audio needed (they play muted).
- MP4 (H.264) format, landscape orientation.
- If a file is missing, that spot just quietly shows the existing icon/design as before - nothing breaks.
